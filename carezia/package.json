{
  "name": "carezia",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "npm run dev --workspace @carezia/web",
    "build": "npm run build --workspace @carezia/web",
    "test": "npm run test --workspace @carezia/core",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspace @carezia/web"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=20"
  }
}
